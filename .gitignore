.gradle/
.kotlin/
**/build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
*.log
3side/

# Local tooling artifacts (Codex remote attachment bridge)
.codex-remote-attachments/

# Locally built installers: kept on disk for testing, never committed (apk/README.md stays).
apk/*.apk

# Promo signing key: publisher-side only, never committed.
*.key

# Release signing material must never be committed.
keystore.properties
*.jks
*.keystore
!gradle/wrapper/gradle-wrapper.jar
