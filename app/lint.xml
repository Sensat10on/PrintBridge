<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        False positive: local.properties is generated by the Android tooling and is
        git-ignored. Both `sdk.dir=C\:\\path` and `sdk.dir=C:\\path` are valid property
        files and both are accepted by AGP; lint flags either spelling. Suppressing the
        check here keeps `gradlew lintDebug` usable as a release gate.
    -->
    <issue id="PropertyEscape" severity="ignore" />
</lint>
