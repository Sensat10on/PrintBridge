<?xml version="1.0" encoding="utf-8"?>
<!--
    Internal test build: everything unlocked, so the launcher icon is the TEST artwork and the
    TEST badge makes the build recognisable next to the release one.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:icon="@mipmap/ic_launcher_test"
        android:roundIcon="@mipmap/ic_launcher_test" />
</manifest>
