# TCP Printing

`TcpPrinterTransport` sends raw bytes to a validated host and port. Default RAW printing port is `9100`. The same ESC/POS or TSPL bytes can be sent through fake, TCP, or future Bluetooth/USB transports.
